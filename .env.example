# PostgreSQL connection string (required for local dev and production).
# Render: use the Internal Database URL from your Render Postgres dashboard.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# Port the dev server listens on when running locally (npm run dev). Defaults to 4000.
PORT=4000
